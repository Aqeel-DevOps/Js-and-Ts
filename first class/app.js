// npm install typescript -g (for globaly install)
// to run app.ts file we use command tsc app.ts in cmd.
// we use command for watching the file tsc app.ts -w to watch continuously.
var firstName = "Nomi";
firstName = "Nomi Khan";
console.log(firstName);
